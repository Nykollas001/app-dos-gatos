# ========================================
# 🚀 VARIÁVEIS DE AMBIENTE - BACKEND
# ========================================
# Copie este arquivo para .env e preencha com seus valores

# Porta do servidor
PORT=3001

# Ambiente (development, production, test)
NODE_ENV=production

# JWT Secret (gere uma chave segura!)
# Exemplo: openssl rand -base64 32
JWT_SECRET=sua_chave_secreta_super_segura_aqui_123456789

# URL do banco de dados (quando migrar para PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost:5432/app_dos_gatos

# CORS - URLs permitidas para requisições
CORS_ORIGIN=http://localhost:5173,https://seu-frontend-vercel.vercel.app

# Modo Debug
DEBUG=false

# ========================================
# 📝 INSTRUÇÕES
# ========================================
# 1. Copie este arquivo: cp .env.example .env
# 2. Preencha os valores com seus dados
# 3. NUNCA commite o arquivo .env no Git!
# 4. Adicione .env ao .gitignore (já está)
# ========================================
